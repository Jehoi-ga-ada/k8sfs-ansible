---
- name: Bootstrap a mac for the cluster
  hosts: bootstrap
  roles:
    - role: elliotweiser.osx-command-line-tools
      become: true
    - role: sudo_user
    - role: tailscale
      tags: tailscale
      when: use_tailscale | default(false) | bool
